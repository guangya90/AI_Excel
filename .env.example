# --- Supabase（推荐：连接池 + 直连）---
# 将 [YOUR-PASSWORD] 换成 Supabase 项目设置里的数据库密码；勿提交真实 .env。

# 连接池（应用 / Serverless，端口 6543）
DATABASE_URL="postgresql://postgres.htokosarzqvnkahqpxnv:[lgy891002lgy]@aws-1-ap-southeast-1.pooler.supabase.com:6543/postgres?pgbouncer=true"

# 直连（Prisma migrate / db push，端口 5432）
DIRECT_URL="postgresql://postgres.htokosarzqvnkahqpxnv:[lgy891002lgy]@aws-1-ap-southeast-1.pooler.supabase.com:5432/postgres"

# --- 其他 Postgres（Neon / 自建）---
# 若只有一条 URL，可把 DATABASE_URL 与 DIRECT_URL 设为相同连接串，或仅保留 url、去掉 schema 里的 directUrl（不推荐 Supabase 这么做）。
